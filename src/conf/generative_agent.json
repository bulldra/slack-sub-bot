{
	"type": "function",
	"function": {
		"name": "generate_agent",
		"description": "エージェントを生成する。会話なら /gpt, 画像なら /image, アイデアなら /idea, 調査なら /research を指定する。",
		"parameters": {
			"type": "object",
			"properties": {
				"agent": {
					"type": "string",
					"description": "生成されたエージェント名",
					"enum": ["/gpt", "/image", "/idea", "/search"]
				}
			},
			"required": ["synonyms"]
		}
	}
}
